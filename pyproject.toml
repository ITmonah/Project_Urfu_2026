[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = [
    ".venv",
    "artifacts",
    "dataset",
    "datasets",
    "datasetCreation",
    "runs",
]

[tool.pytest.ini_options]
addopts = "--basetemp=.pytest_tmp"
testpaths = ["tests"]
pythonpath = ["."]
